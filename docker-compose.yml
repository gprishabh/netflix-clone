version: '3.8'

services:
  backend:
    build: ./netflix-api
    ports:
      - "5000:5000"
    container_name: netflix-api
    restart: always

  frontend:
    build: ./netflix-ui
    ports:
      - "3000:3000"
    container_name: netflix-ui
    restart: always
    depends_on:
      - backend

